.env
*.env.local
bin/
# dist is build output, but keep .gitkeep so go:embed matches on a fresh clone
web/dist/*
!web/dist/.gitkeep
web/node_modules/
*.test
.DS_Store
scripts/node_modules/
# e2e screenshots: the check scripts write to tmp/ (or $OUT), never the repo root
/*.png
scripts/**/*.png
tmp/
# prod ssh host and health URL for scripts/deploy-prod.sh
scripts/deploy-prod.env
/exports/
