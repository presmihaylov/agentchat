# 03 Room users schema, room creation requires login, session room entry

Status: todo

## Scope
- Migration 000025 (design section 6): `participants.user_id` with the partial unique index `(room_id, user_id) WHERE user_id IS NOT NULL` and the user index, `rooms.created_by_user_id` with its index, `users.last_active_room_id` FK to `rooms` (the column exists since 000024; its JSON key is `last_active_workspace_id`), and `participants.token_hash` DROP NOT NULL (down restores a placeholder hash, then SET NOT NULL). `CreateRoomAs` needs the nullable token on this deploy.
- Models: `Participant.UserID`, `Room.CreatedByUserID`, `User.LastActiveWorkspaceID` (scanned from `users.last_active_room_id`); `CreateParticipant` gains a trailing `userID *string` and accepts a nil token hash; `handleJoinRoom` passes nil; new `CreateRoomAs(ctx, name, slug, secret, creator)` (room, admin participant linked to the user, #general, `participant.joined` with `user_id`, one tx, room lock first). The creator's participant name is `display_name` when `validParticipantName` accepts it, else `username`, then `-2`, `-3` against existing rows.
- `POST /api/v1/rooms` requires a `ses_` session through `withSession`: 401 `session_required` for no token and for `act_` (one code). Cap 5 rooms per creator under `pg_advisory_xact_lock(hashtext('room-create:' || user_id))`, 409 `workspace_quota`. Agents cannot create rooms.
- `authed()` session branch: `participantForSession` reads `X-Workspace-Slug`, one query `SessionScope`; outcomes per design section 4 (400 `workspace_required`, 404, 403 `workspace_forbidden` with `reason: not_member` or `revoked`); updates `users.last_active_room_id` on change. The task 01 `no_room` placeholder goes.
- `POST /api/v1/workspaces/{slug}/enter` with `invite_code`: live participant idempotent, revoked 403, non-member needs a code that opens this slug (else 400 `invite_invalid`), creates a participant linked to the user, never adopts by name.
- `ReclaimParticipant` refuses rows with `user_id IS NOT NULL` (ErrConflict, same as revoked). Agents keep `user_id NULL`, so the agent `/join` path is byte-identical.
- SPA (`web/src/app.js`): the `/create` block posts `POST /api/v1/rooms` with the session and lands on `/r/<slug>`; the "Your name" field goes and the create no longer joins with the code. On `/r/{slug}` a session is sent when no per-slug `act_` token exists; `workspace_forbidden` routing in `api()` shows `#enter-view` (invite code only) for `not_member` and the removed notice for `revoked`; the two 401 reload sites route through the same function; the legacy per-slug token key is deleted once a session works. `#join-view` stays for humans without a session until task 05.
- Remove the unauthenticated room create everywhere, in this change (design section 8 lists the files): `services/api/skill.go` "## Creating a new room" text (agents cannot create rooms; ask your human for a workspace invite code; a `/join` cannot reclaim a human who logs in) with `TestSkillDoc` and `TestSkillHarnessGuides`, `services/api/api_test.go` `setupRoom` and the three other create sites, `scripts/cli-e2e.sh` room setup, `scripts/e2e.sh` through `cmd/agentchat create-room --session`, new `scripts/lib/login.js` (`registerAndLogin`, `createRoom`), and the 48 browser scripts that post to `/api/v1/rooms`.
- Migration note in #agents-backstage before this deploy (removed unauthenticated create, `user_id` in `participant.joined`).

## Acceptance
- Go tests: `TestRoomCreateRequiresSession` (none 401 `session_required`, `act_` 401 `session_required`, `ses_` 201 with the creator as admin and `user_id` set), `TestRoomCreateNameFromInvalidDisplayName` (a 40 char or emoji display name yields the username as participant name), `TestRoomQuota` (sixth create 409), `TestAgentJoinRowUnchanged`, `TestJoinCannotReclaimLinkedHuman`, `TestSessionAuthResolvesParticipant`, `TestSessionWithoutParticipantIs403`, `TestRevokedHumanIs403FromEnterAndRoom`, `TestEnterWithInviteCodeCreatesLinkedParticipant`, `TestEnterDoesNotAdoptByName`, `TestEnterWrongCodeIs400`, `TestActTokenIgnoresRoomHeader`, `TestSkillDoc`, `TestSkillHarnessGuides`.
- `scripts/cli-e2e.sh` and `scripts/e2e.sh` pass with only their room-setup lines changed; `git diff services/api/cli.sh` empty; every scripts/*-check.js passes through `scripts/lib/login.js`; `scripts/login-check.js` covers create, land on `/r/<slug>` as admin, enter a second workspace with a code.
- Rollback rehearsal on a dev DB: `-migrate-to 24`, then the task 02 binary starts.
- Deployed with `AGENTCHAT_REGISTRATION_ENABLED=false` still set on prod (opens at task 04).
